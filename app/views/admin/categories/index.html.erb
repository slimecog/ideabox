<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.title, admin_category_path(category) %></li>
    <%= link_to "Delete", admin_category_path(category), method: :delete %>
  <% end %>
</ul>
